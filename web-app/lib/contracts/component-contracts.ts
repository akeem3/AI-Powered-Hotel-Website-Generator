// contracts/component-contracts.ts
// ------------------------------------------------------
// Central registry for UI component-level contracts
// (Button, Input, Select, Textarea, Badge, etc.)
// Add schemas here as you create them.
// ------------------------------------------------------

export const UIComponentContracts = {
  // Example placeholder (uncomment when ready):
  // Button: ButtonContract,
  // Input: InputContract,
};

export type UIComponentContractMap = typeof UIComponentContracts;
